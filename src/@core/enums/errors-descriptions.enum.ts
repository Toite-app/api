/**
 * Enumeration of errors responses descriptions
 */
export enum errorsDescriptions {
  badRequest = "Wrong request parameters",
  forbidden = "Not enough rights to access",
  serverErr = "Server error",
}
