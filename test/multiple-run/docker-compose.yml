version: "3.8"

services:
  app1:
    build:
      context: ../..
      dockerfile: test/multiple-run/Dockerfile
      target: runner
    environment:
      - PORT=3001
    env_file:
      - ../../.env
    ports:
      - "3001:3001"
    volumes:
      - ../../src:/app/src:ro
    network_mode: "host"

  # app2:
  #   build:
  #     context: ../..
  #     dockerfile: test/multiple-run/Dockerfile
  #     target: runner
  #   env_file:
  #     - ../../.env
  #   environment:
  #     - PORT=3002
  #   ports:
  #     - "3002:3002"
  #   network_mode: "host"

volumes:
  app1_node_modules:
  app2_node_modules:
